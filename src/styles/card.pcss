.card {
    border: 1px solid #fafafa;
    padding: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.25);
    border-radius: var(--border-radius);

    &__footer {
        margin-top: 10px;
        margin-bottom: -10px;
        text-align: center;
    }
}
