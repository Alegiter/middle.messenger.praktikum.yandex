@import '../../styles/base.pcss';
@import '../../styles/card.pcss';
@import '../../core/components/button/button.pcss';
@import '../../core/components/input/input.pcss';
@import '../../core/components/form/form.pcss';
@import '../../core/components/header/header.pcss';
@import 'components/editable-avatar/editable-avatar.pcss';

.profile {
    &__image {
        @extend .flex;
        @extend .flex_centered;
        flex-direction: column;
        margin-bottom: 24px;
    }

    &__name {
        @extend .title;
        @extend .flex;
        @extend .flex_centered;
    }

    &__form {
        /* todo */
    }

    &__footer {
        margin-top: 10px;
        margin-bottom: -10px;

        &-item {
            margin-bottom: 5px;
        }
    }
}
